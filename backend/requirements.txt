# LedgerLens backend
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6

pdfplumber>=0.11
pandas>=2.2
pytesseract>=0.3.10
Pillow>=10.0
openpyxl>=3.1

# Alternative/supplementary PDF extraction backends
pymupdf>=1.24
camelot-py>=1.0
ocrmypdf>=15.4

# Optional: only needed if you also want XLS (legacy) input support
# xlrd>=2.0
opencv-python-headless>=4.9  # scan clean-up (lighting, ruling lines); OCR still runs without it
